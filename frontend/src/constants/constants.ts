// Path to static files
const STATIC_URL = "./public"

// Backend connection ports
const WS_SERVER_HOST = "ws:localhost:8082"

// Canva view div id
const ROOT_DIV_ID = "#root"

// Default background color
const MAIN_BG_COLOR = 0x18215d;

// Container's dimesnsion
const CONTAINER_DIM = 1440;

export { STATIC_URL, WS_SERVER_HOST, ROOT_DIV_ID, MAIN_BG_COLOR, CONTAINER_DIM };