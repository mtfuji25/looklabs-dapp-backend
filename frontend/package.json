{
  "name": "frontend",
  "version": "1.5.0",
  "description": "In this game you are going to explore an amazing world and create you own journey through the depths of a dark and frightening forest.",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run server && npm run build",
    "build": "webpack",
    "server": "./node_modules/.bin/http-server -c-1 -p 8080 &",
    "stop": "pkill -f http-server",
    "clean": "rm -f ./public/*.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "file-loader": "^6.2.0",
    "http-server": "^13.0.2",
    "webpack": "^5.57.1",
    "webpack-cli": "^4.8.0"
  },
  "dependencies": {
    "pixi.js": "^6.1.3"
  }
}
