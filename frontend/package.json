{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run server && npm run devlop",
    "devlop": "webpack --config webpack.development.config.js",
    "build": "webpack --config webpack.production.config.js",
    "server": "./node_modules/.bin/http-server -c-1 -p 8080 &",
    "deploy": "firebase deploy --only hosting:game --project default",
    "stop": "pkill -f http-server",
    "clean": "rm -f ./public/*.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.0.0",
    "ts-loader": "^9.2.6",
    "typescript": "^4.4.4",
    "webpack": "^5.61.0",
    "webpack-cli": "^4.9.1"
  },
  "dependencies": {
    "pixi.js": "^6.2.0"
  }
}
